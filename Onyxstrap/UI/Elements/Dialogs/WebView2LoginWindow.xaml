<base:WpfUiWindow x:Class="Onyxstrap.UI.Elements.Dialogs.WebView2LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:base="clr-namespace:Onyxstrap.UI.Elements.Base"
        mc:Ignorable="d"
        Title="Log in to Roblox"
        Width="1020"
        Height="740"
        MinWidth="720"
        MinHeight="540"
        Background="{ui:ThemeResource ApplicationBackgroundBrush}"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterScreen"
        Loaded="WebView2LoginWindow_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Padding="8" Title="Log in to Roblox" ShowMinimize="False" ShowMaximize="True" Icon="pack://application:,,,/Onyxstrap.ico" />

        <Border Grid.Row="1" Padding="14,8" Background="{DynamicResource SolidBackgroundFillColorTertiaryBrush}">
            <StackPanel Orientation="Horizontal">
                <ui:SymbolIcon Symbol="LockClosed16" Foreground="{DynamicResource SystemAccentColorPrimaryBrush}" />
                <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" FontSize="13"
                           Text="Log in to your Roblox account below. Onyxstrap captures the session automatically when login completes, then encrypts it locally - your password is never seen or stored by Onyxstrap." />
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" x:Name="WebViewHost">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ui:ProgressRing IsIndeterminate="True" Width="44" Height="44" />
                <TextBlock Margin="0,12,0,0" HorizontalAlignment="Center" FontSize="13"
                           Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                           Text="Loading the Roblox login page..." />
            </StackPanel>
        </Grid>
    </Grid>
</base:WpfUiWindow>
