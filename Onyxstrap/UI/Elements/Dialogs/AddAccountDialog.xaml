<base:WpfUiWindow x:Class="Onyxstrap.UI.Elements.Dialogs.AddAccountDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:Onyxstrap.UI.Elements.Dialogs"
        xmlns:base="clr-namespace:Onyxstrap.UI.Elements.Base"
        xmlns:resources="clr-namespace:Onyxstrap.Resources"
        mc:Ignorable="d"
        Title="{x:Static resources:Strings.Dialog_AddAccount_Title}"
        MinHeight="0"
        Width="560"
        SizeToContent="Height"
        ResizeMode="NoResize"
        Background="{ui:ThemeResource ApplicationBackgroundBrush}"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterScreen">
    <base:WpfUiWindow.Resources>
        <QuadraticEase x:Key="SilkEase" EasingMode="EaseInOut" />
    </base:WpfUiWindow.Resources>
    <base:WpfUiWindow.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="EntranceHost" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.25" />
                    <DoubleAnimation Storyboard.TargetName="EntranceSlide" Storyboard.TargetProperty="Y" From="14" To="0" Duration="0:0:0.3" EasingFunction="{StaticResource SilkEase}" />
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </base:WpfUiWindow.Triggers>
    <Grid x:Name="EntranceHost">
        <Grid.RenderTransform>
            <TranslateTransform x:Name="EntranceSlide" Y="0" />
        </Grid.RenderTransform>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Grid.ColumnSpan="2" Padding="8" Title="{x:Static resources:Strings.Dialog_AddAccount_Title}" ShowMinimize="False" ShowMaximize="False" CanMaximize="False" KeyboardNavigation.TabNavigation="None" />

        <Grid Grid.Row="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" MinWidth="110" Text="{x:Static resources:Strings.Dialog_AddAccount_NameLabel}" Margin="0,0,12,12" />
            <TextBox Grid.Row="0" Grid.Column="1" Name="AccountNameTextBox" Margin="0,0,0,12" />

            <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" TextWrapping="Wrap" Text="{x:Static resources:Strings.Dialog_AddAccount_TokenLabel}" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Margin="0,0,0,12" />

            <PasswordBox Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Name="AccountTokenTextBox" />
        </Grid>

        <Border Grid.Row="2" Margin="0,10,0,0" Padding="15" Background="{ui:ThemeResource SolidBackgroundFillColorSecondaryBrush}">
            <StackPanel Orientation="Horizontal" FlowDirection="LeftToRight" HorizontalAlignment="Right">
                <Button x:Name="OKButton" MinWidth="100" Content="{x:Static resources:Strings.Dialog_AddAccount_OK}" IsEnabled="False" Click="OKButton_Click" />
                <Button MinWidth="100" Margin="12,0,0,0" Content="{x:Static resources:Strings.Common_Cancel}" IsCancel="True" />
            </StackPanel>
        </Border>
    </Grid>
</base:WpfUiWindow>
