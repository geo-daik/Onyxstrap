<Window x:Class="Onyxstrap.UI.Elements.Bootstrapper.OnyxDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Width="600"
        Height="380"
        ResizeMode="NoResize"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        Background="Transparent"
        Closing="Window_Closing">

    <Window.TaskbarItemInfo>
        <TaskbarItemInfo ProgressState="{Binding Path=TaskbarProgressState}" ProgressValue="{Binding Path=TaskbarProgressValue}" />
    </Window.TaskbarItemInfo>

    <Window.Resources>
        <QuadraticEase x:Key="SilkEase" EasingMode="EaseInOut" />

        <!-- gem breathing loop -->
        <Storyboard x:Key="GemLoop" RepeatBehavior="Forever" AutoReverse="True">
            <DoubleAnimation Storyboard.TargetName="GemScale" Storyboard.TargetProperty="ScaleX" From="1" To="1.06" Duration="0:0:1.6" EasingFunction="{StaticResource SilkEase}" />
            <DoubleAnimation Storyboard.TargetName="GemScale" Storyboard.TargetProperty="ScaleY" From="1" To="1.06" Duration="0:0:1.6" EasingFunction="{StaticResource SilkEase}" />
            <DoubleAnimation Storyboard.TargetName="GemGlow" Storyboard.TargetProperty="BlurRadius" From="24" To="44" Duration="0:0:1.6" EasingFunction="{StaticResource SilkEase}" />
            <DoubleAnimation Storyboard.TargetName="GemGlow" Storyboard.TargetProperty="Opacity" From="0.55" To="0.95" Duration="0:0:1.6" EasingFunction="{StaticResource SilkEase}" />
        </Storyboard>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource GemLoop}" />
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="RootBorder" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5" />
                    <DoubleAnimation Storyboard.TargetName="Gem" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.9" />
                    <DoubleAnimation Storyboard.TargetName="Wordmark" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.7" BeginTime="0:0:0.35" />
                    <DoubleAnimation Storyboard.TargetName="Tagline" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.7" BeginTime="0:0:0.55" />
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Grid>
        <Border x:Name="RootBorder" CornerRadius="18" BorderBrush="#268A80DC" BorderThickness="1" Opacity="0">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FF1B1728" Offset="0" />
                    <GradientStop Color="#FF100E19" Offset="0.55" />
                    <GradientStop Color="#FF0B0912" Offset="1" />
                </LinearGradientBrush>
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect BlurRadius="42" ShadowDepth="0" Opacity="0.65" Color="#140C28" />
            </Border.Effect>

            <Grid ClipToBounds="True">
                <!-- ambient glows -->
                <Ellipse Width="360" Height="360" Margin="-140,-160,0,0" HorizontalAlignment="Left" VerticalAlignment="Top">
                    <Ellipse.Fill>
                        <RadialGradientBrush>
                            <GradientStop Color="#4D6C5CE7" Offset="0" />
                            <GradientStop Color="#006C5CE7" Offset="1" />
                        </RadialGradientBrush>
                    </Ellipse.Fill>
                </Ellipse>
                <Ellipse Width="300" Height="300" Margin="0,0,-120,-140" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                    <Ellipse.Fill>
                        <RadialGradientBrush>
                            <GradientStop Color="#33483CA8" Offset="0" />
                            <GradientStop Color="#00483CA8" Offset="1" />
                        </RadialGradientBrush>
                    </Ellipse.Fill>
                </Ellipse>

                <!-- close button -->
                <Button VerticalAlignment="Top" HorizontalAlignment="Right" Width="34" Height="34" Margin="12"
                        BorderThickness="0" Foreground="#66E8E6F2" FontSize="14" FontFamily="{StaticResource Rubik}"
                        Content="✕" Visibility="{Binding CancelButtonVisibility, Mode=OneWay}" Command="{Binding CancelInstallCommand}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Hover" CornerRadius="8" Background="Transparent">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Hover" Property="Background" Value="#22E8E6F2" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- centerpiece -->
                <StackPanel VerticalAlignment="Center" Margin="0,0,0,60">
                    <Image x:Name="Gem" Width="104" Height="104" HorizontalAlignment="Center"
                           Source="pack://application:,,,/Onyxstrap.ico" RenderOptions.BitmapScalingMode="HighQuality">
                        <Image.RenderTransform>
                            <ScaleTransform x:Name="GemScale" CenterX="52" CenterY="52" />
                        </Image.RenderTransform>
                        <Image.Effect>
                            <DropShadowEffect x:Name="GemGlow" BlurRadius="24" ShadowDepth="0" Color="#8A80DC" Opacity="0.55" />
                        </Image.Effect>
                    </Image>

                    <TextBlock x:Name="Wordmark" HorizontalAlignment="Center" Margin="0,18,0,0" Opacity="0"
                               Text="O N Y X S T R A P" FontFamily="{StaticResource Rubik}" FontSize="24" FontWeight="SemiBold">
                        <TextBlock.Foreground>
                            <LinearGradientBrush>
                                <GradientStop Color="#FFE8E6F2" Offset="0" />
                                <GradientStop Color="#FFB9AFFF" Offset="1" />
                            </LinearGradientBrush>
                        </TextBlock.Foreground>
                    </TextBlock>

                    <TextBlock x:Name="Tagline" HorizontalAlignment="Center" Margin="0,4,0,0" Opacity="0"
                               Text="forged for Roblox" FontFamily="{StaticResource Rubik}" FontSize="12"
                               Foreground="#99B9AFFF" />
                </StackPanel>

                <!-- status + progress -->
                <Grid VerticalAlignment="Bottom" Margin="60,0,60,44">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" TextAlignment="Center" Text="{Binding Message}" FontFamily="{StaticResource Rubik}"
                               FontSize="15" FontWeight="Light" Foreground="#CCE8E6F2" Margin="0,0,0,18" />

                    <ProgressBar Grid.Row="1" Height="6" BorderThickness="0"
                                 Foreground="#FF8A80DC" Background="#14FFFFFF"
                                 IsIndeterminate="{Binding ProgressIndeterminate}"
                                 Maximum="{Binding ProgressMaximum, Mode=OneWay}" Value="{Binding ProgressValue}">
                        <ProgressBar.Template>
                            <ControlTemplate TargetType="ProgressBar">
                                <Border x:Name="PART_Track" CornerRadius="3" Background="{TemplateBinding Background}">
                                    <Border x:Name="PART_Indicator" CornerRadius="3" HorizontalAlignment="Left" Background="{TemplateBinding Foreground}" />
                                </Border>
                            </ControlTemplate>
                        </ProgressBar.Template>
                    </ProgressBar>
                </Grid>

                <TextBlock VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="16"
                           FontSize="10" FontFamily="{StaticResource Rubik}" Foreground="#55E8E6F2"
                           Text="{Binding VersionText}" Visibility="{Binding VersionTextVisibility, Mode=OneWay}" />
            </Grid>
        </Border>
    </Grid>
</Window>
